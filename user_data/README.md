# User Data

The editor stores recoverable local state here:

- `progress.json` contains photo labels and source-save identity metadata.
- `settings.json` contains remembered file selections and UI preferences.
- `photos/` contains photos extracted from the selected vanilla save.

These files are not required to run a fresh copy of the editor.
