# Spoiler Assets

`entities.json` contains the entity list used by the editor. `entity_icons/`
contains the matching bestiary previews used by the mapping interface.
The player's extracted photos are stored separately under `user_data/photos/`.
